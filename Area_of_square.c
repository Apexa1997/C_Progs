// Area of square
# include <stdio.h>
int main()

{
    int side;
    printf("Enter the value of side: ");        // Value taken from user
    scanf("%d", &side);
    printf("Area of square: %d", side * side);    // Calculate are of square
    return 0;
}